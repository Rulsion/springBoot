spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
      server:
        git:
          uri: https://gitee.com/Rulsion/config-repo
          search-paths: licensingservice,oaganizationservice
          username: Rulsion
          password: Rulsion38433623

